import { NumberGenerator } from './NumberGenerator';
import { GroupArrayByKey } from './GroupArraysByKey';
import { NumberGenerator } from './NumberGenerator';

const Utilities = {
  NumberGenerator,
  GroupArrayByKey,
  NumberGenerator,
};

export default Utilities;
